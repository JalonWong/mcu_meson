project('mcu_app', ['c', 'cpp'],
    default_options: [
        'default_library=static',
        'optimization=1',
        'debug=true',
    ]
)

library('app',
    'src/lib.c',
    'src/lib.cc',
)
